# Frontend production environment (example — copy to .env.production and fill in)
# The deployed API base URL (Azure App Service). No trailing slash.
VITE_API_URL=https://REPLACE_WITH_API_NAME.azurewebsites.net
